DROP DATABASE IF EXISTS geolocate;
CREATE DATABASE geolocate;

CREATE TABLE geolocate.pos (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  ts TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  name VARCHAR(50),
  x FLOAT,
  y FLOAT 
);

SELECT * FROM geolocate.pos ORDER BY ts DESC LIMIT 1;